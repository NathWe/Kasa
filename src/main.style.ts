import styled from "styled-components";

// Créez des composants stylisés pour chaque section du fichier CSS

// Page styles
export const PageStyles = styled.div`
  color: #ff6060;
  background: white;
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
  font-family: "Montserrat", sans-serif;
`;

// Reset styles
export const ResetStyles = styled.div`
  padding: 0;
  margin: 0;
  box-sizing: border-box;
`;

// Liste styles
export const ListStyles = styled.li`
  list-style-type: none;
`;
